Applies the eligible upstream changes after transforming both upstream snapshots into Ruby names. The exact consumed commit range is recorded in `maintenance/upstream/state.json`.

Ruby marketing, branding assets, and maintenance/deployment policy are protected. The accepted cursor changes only when this PR merges. Human review and a passing **Ruby release gate** check are required. Nothing is automatically merged or deployed.
